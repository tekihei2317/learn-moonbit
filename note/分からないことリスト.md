# 分からないことリスト

今作っているプロジェクトで、少ししかコードは書いていませんが分からないことがたくさん出てきたので、それをここに転記しておきます。随時追記します。

- プロジェクトを作るときにどうすればいいか？
  - [moonbitプロジェクトの作り方](./moonbitプロジェクトの作り方.md)
- moon newにテンプレートを指定することはできるか？最小限のものは何か？
- 文字列
  - 文字列をsplitする方法
  - 文字列を数値に変換する方法
  - 関数の引数は、StringとStringViewどちらにするのが良いか。Stringで書いているが、計算しているとStringViewがよく出てくる。
  - StringView型にString型を渡せるか。渡せるっぽい。
  - 文字列をループするにはどうすればいい？
- 配列
  - Arrayにmapはあるか
- Option型
  - Option型で、SomeかNoneかを判別する方法。
  - Array[T?]をfilterでArray[T]に絞り込めないのか？これはfilter_mapを使うといいらしい。
- パッケージ、モジュール
  - パッケージをインポートする方法。@strcnv.parse_intを使いたい
  - 一つのファイルにパッケージをインポートすることはできないのか
  - パッケージをインストールする方法。
    - [moonbitlang/x Now Supports File I/O | MoonBit](https://www.moonbitlang.com/blog/io-support)
  - ファイルを読み込む方法。
    - `moon add moonbitlang/x`
  - `moonbitlang/x`は何か
  - 他のファイルから関数をインポートするには？
  - 同じパッケージ内の別ファイルの関数を使えるやつ、ES Modulesの感覚だとグチャグチャになりそうな印象があるけど大丈夫だろうか？そのファイルだけで使用するプライベートな関数って定義できないのかな。
  - rabbit-TEAをインストールする方法は？
- 標準ライブラリ
  - cliの引数を受け取るにはどうすれば良いか。
  - sysパッケージはどこにあるか。
- 文法
  - 三項演算子はないのか。
  - `catch { _ => ...  }`と`catch { _ => return ... }`は意味が違う？
  - 別のスコープで、同じ名前の変数を宣言して上書きできるやつ、なんて言うんだっけ
  - StructのConstructorみたいなやつってある？補完のために使いたい。`Todo { id, done, text }`みたいなやつ
  - メソッド呼び出しがあるらしい。
  - 構造体のフィールドが関数のとき、`(instance.func)()`みたいにカッコをつけて呼び出さないといけないの？
- テスト
  - テストをグルーピングするにはどうすればいいか。
  - ブラックボックステストとは何か。
  - テストは同一ファイルに書いているが、関数→テスト→関数→テストと近くに書くのと、一番下に書くのとどちらが良いか。
  - テストを実行した時に、テストケース名を表示するオプションはあるか。`--verbose`は長すぎるので。
- その他
  - `Unexpected main function in the non-main package.`
  - main.mbtを実行するコマンドは何？
  - もっとテストを書きながら進めた方がいいと言う実感はあるけど、具体的にどうすればいいかがわからない
  - エラーハンドリングとか、それをユーザー側に伝えるための方法を知らなすぎて、実装イメージが湧いていないのがよくない
- エラー extern "js" is unsupported in wasm-gc backend.が出る
- ドキュメントのtestで使われてるinspectってなんだろう
  - [Moonbit moon cli の便利機能達 - 組み込み lint, bench, snapshot, coverage, doc](https://zenn.dev/mizchi/articles/moonbit-useful-tips)
- Mapの初期値ってどうなってる？
- 改めて、単純にあるモジュールにどういう関数が用意されているかが分かればその場に最適なコードが書けるわけではないっぽいと思った
  - 実現したいことに対して書き方が一つというわけではないので、思い浮かばない学習の最初のステップでは、色々な方向性を吟味する必要がある
  - 完全に人力でやるのは効率が良くないので、AIに必要な情報を渡しならが学習のループを回すとよさそう。
  - まだ新らし目な言語のため変なこと言ってることもあるので、ループに検証を組み込む必要がある。
- Moonbitでライブラリを作ってmooncakes.ioに公開するにはどうすれば良いか。
- ライブラリを作るとき、モジュールをどう配置するか
  - `moon.pkg.json`と同じディレクトリにある、`pub`をつけたものが公開されるはず
  - [自分が Moonbit 言語について知っていること](https://zenn.dev/mizchi/articles/practical-moonbit#%E3%83%A9%E3%82%A4%E3%83%96%E3%83%A9%E3%83%AA%E3%81%AE%E4%BD%9C%E3%82%8A%E6%96%B9)
- ライブラリのドキュメンテーションコメントは、どのように書くのが良いか
- StringViewをスライスするといちいちエラーが発生するのが面倒くさい、どうすればい？

## Rabbit-TEA

- Rabbit-TEAで、コンポーネントを作って組み合わせるにはどうすれば良いか
  - model、view,、updateを適切に分割して合成する、という理解でとりあえず進む
- @html.Html[M]の、ジェネリクスは何を意味しているのか
  - Msgです
- キーボード入力を受け取るにはどうすればいいか？
- タイマーはどう実装すればいいか？
- Rabbit-TEAで関数をModelに入れたくなったらどうするか？
